#include "main.h"
#include <stdio.h>
/**
 *main - Entry point
 *@argc: intger variable
 *@argv: array variable to string
 *Return: always 0 (success)
 */
int main(int argc, char *argv[])
{
  int i = 0;
  for (; i < argc; i++)
    {
      printf("%s", argv[i]);
    }
  return (0);
}

