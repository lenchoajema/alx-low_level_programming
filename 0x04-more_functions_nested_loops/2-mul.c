#include "main.h"
/**
 *mul - multiplication operation
 *Return: always return the result
 *@a: first variable to multiplie
 *@b: second variable to multiplie
 */
int mul(int a, int b)
{
int mul = 0;
mul = a * b;
return (mul);
}
